# from lambda.aws.lapi_event_translator import RequestRouter

# import pytest
# from mock import patch, Mock

# class MockRequestProcessor:
#     def __init__(self):
#         pass

# def provide_requestProcessor():
#     requestProcessor = MockRequestProcessor()

#     yield requestProcessor


# def test_route_request_getContactById():
#     """
#     GIVEN a 'RequestRouter' initialized with a 'MockRequestProcessor',
    
#     WHEN an even JSON string representing a getContactById request is given 
#          to the request router,
    
#     THEN the appropriate method of the MockRequestProcessor is called with the 
#          expected args, and the request router returns the expected 
#     """
    